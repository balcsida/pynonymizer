scripts:
  before: 
    - SELECT * FROM actor LIMIT 1;
  after: 
    - SELECT * FROM actor LIMIT 1;
    
tables:
  film:
    columns:
      description: bs
      title: word
  actor:
    columns:
      first_name:
        type: literal
        value: (SELECT title from film WHERE film.film_id = actor.actor_id)
      last_name: last_name
  film_text: delete